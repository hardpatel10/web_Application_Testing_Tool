# Tasks

Tracks phase-level progress for the Security Assessment Dashboard. Each
phase requires explicit approval before the next one begins (see
`.claude/CLAUDE.md`).

## Phase 1 — Repository Foundation — Complete

- FastAPI app factory, structured logging, global exception handlers, secret-key strength check.
- Async SQLAlchemy engine/session scaffolding (no models yet).
- `/api/v1/health`, `/api/v1/version`, `/api/v1/system` — all returning live, non-mocked data.
- React + TS + Vite frontend shell: 6 routed pages, dark mode, honest empty states.
- Root project scaffolding: Docker, `.env.example`, `LICENSE`, `README.md`, `docs/ARCHITECTURE.md`.

## Phase 2 — Core Database & Domain Model — Complete

- 14 SQLAlchemy 2.0 ORM models covering the full assessment → target → tool execution → finding lifecycle, plus notes/reports/attachments/settings.
- `backend/models/base.py`: `UUIDPrimaryKeyMixin`, `CreatedAtMixin`, `TimestampMixin` shared across all models.
- `backend/models/enums.py`: 10 `StrEnum` types stored as portable `VARCHAR` (`native_enum=False`), not native DB enums.
- SQLite foreign-key enforcement enabled via a `PRAGMA foreign_keys=ON` connect-event listener (`backend/database/session.py`) — required for `ON DELETE CASCADE`/`RESTRICT` to actually take effect on SQLite.
- Explicit constraint-naming convention on `Base.metadata` (`backend/database/base.py`) so Alembic's SQLite batch-mode migrations can target constraints by name later.
- Generic async repository (`backend/database/repository.py`) + pagination/sort value objects (`backend/database/pagination.py`): `get`/`list`/`count`/`create`/`update`/`delete`, no business logic.
- Initial Alembic migration (`d2bf6f4862f6_create_core_domain_schema.py`), autogenerated, reviewed, and verified: upgrade, downgrade, and re-upgrade all succeed against a real SQLite database; `PRAGMA foreign_key_check` reports zero issues; cascade-delete and RESTRICT-on-delete behavior verified end-to-end through the ORM.
- Existing Phase 1 test suite (3 tests) still passes; app still starts and serves real requests with all models loaded.

**Explicitly not done in Phase 2** (per instructions): no business logic, no services, no API routes for these models, no auth.

## Phase 3 — Assessment Management & Target Management — Complete

**Schema evolution** (`67c183630e38_assessment_and_target_management.py`):
- `AssessmentStatus` (Draft/Ready/Running/Paused/Completed/Cancelled/Archived) and `AssessmentType` (Network/Web Application/API/Mobile/Cloud/Internal/External/Custom) value sets replaced; `TargetType` split `ip` into `ipv4`/`ipv6`.
- New: `Assessment.previous_status` (restore support), `Assessment.deleted_at` (soft delete), `Target.enabled`.
- New tables: `assessment_tags` (composite-PK, per-assessment tags), `assessment_history_entries` (append-only activity log).
- New partial unique index: assessment names unique only among non-deleted rows.
- Fixed a defense-in-depth gap discovered while writing this migration: SQLAlchemy 2.0 defaults `Enum(create_constraint=...)` to `False`, so every enum column since Phase 2 had **no `CHECK` constraint at the database level** — verified with a raw-SQL insert that should have failed and didn't. Every model's `Enum(...)` now sets `create_constraint=True`; this migration retrofits the missing `CHECK` onto all of them, not just the three being renamed.
- `downgrade()` has one known, documented limitation on the `assessments` table (see the migration's docstring) — `upgrade()` is fully verified.

**Backend**:
- `backend/utils/target_validators.py`: stdlib-only (`ipaddress`, `urllib.parse`, `re`) validation/normalization/auto-detection for IPv4/IPv6/CIDR/hostname/domain/URL.
- `backend/services/assessment_service.py` + `target_service.py`: full CRUD, search/sort/filter/pagination, archive/restore (with prior-status memory), duplicate (assessment clones tags+targets; target auto-suffixes hostname/domain/URL values, refuses to fabricate IP/CIDR duplicates), soft delete (DB row and on-disk files both preserved), TXT/CSV bulk import with a per-line summary, CSV/TXT export, standalone dry-run validation.
- `backend/core/paths.py`: creates the required `data/assessments/{uuid}/{raw,parsed,reports,screenshots,logs,exports,attachments}` tree on assessment creation and duplication.
- 20 new REST endpoints under `/api/v1/assessments` and `/api/v1/assessments/{id}/targets` (see `backend/api/routes/assessments.py`, `targets.py`).
- `get_db_session` now commits once per successful request / rolls back on exception (completes the transaction-boundary design from Phase 2).
- 21 backend tests (up from 3), all passing, running against an isolated throwaway SQLite DB and directory root (`backend/tests/conftest.py`) — the real `data/app.db` is never touched by tests.
- Full live demonstration against the real dev database: create → add targets (all 4 types) → bulk import (with duplicates/invalid entries) → export → update/archive/restore/duplicate/history → soft delete, all verified via direct HTTP calls with real responses.

**Frontend**:
- 14 new shadcn/ui-style primitives (dialog, alert-dialog, select, switch, dropdown-menu, tabs, badge, table, pagination, skeleton, sonner toaster, input, textarea, label), added `tailwindcss-animate`.
- `Assessments` (list: search/filter/sort/pagination, create dialog, row actions) and new `AssessmentDetails` (Overview/Targets/History tabs, edit/archive/restore/duplicate/delete, target CRUD, bulk import dialog, CSV export) pages.
- TanStack Query hooks (`useAssessments.ts`, `useTargets.ts`) with toast feedback on every mutation.
- `npm run build` and `npm run lint` both clean (1 harmless Fast-Refresh warning, standard for the shadcn Button pattern). Verified end-to-end via the Vite dev server proxying to the live backend and returning real data; **no browser automation tool was available in this environment, so the rendered UI was not visually verified** — only build/type-check/lint and network-level integration.

**Explicitly not done in Phase 3** (per instructions): no plugin framework, no tool execution, no task queue, no background workers, no reports, no findings.

## Phase 4 — Plugin Framework & Plugin SDK — Complete

Builds the complete plugin *architecture* only — no security tool is integrated and no tool is ever executed. New package `backend/plugins/`:

- `models/`: `PluginManifest` (the `plugin.json` schema), `PluginMetadata` (runtime descriptor, always derived via `PluginMetadata.from_manifest`), `PluginHealth`, `PluginConfiguration`, `PluginExecutionContext`/`PluginRawOutput`, `PluginValidationResult`. Reuses `backend.models.enums.TargetType`/`RawOutputFormat` (pure, DB-agnostic `StrEnum`s) rather than duplicating them; only genuinely new concepts (`SupportedPlatform`, `PluginHealthStatus`) get plugin-owned enums.
- `exceptions/`: `PluginLoadError`, `PluginValidationError`, `PluginExecutionError`, `PluginConfigurationError`, `PluginDependencyError`, `PluginNotFoundError` — a framework-internal hierarchy with zero FastAPI/HTTP dependency.
- `core/base_plugin.py`: `BasePlugin` abstract class with all 11 required methods (`metadata`, `health`, `check_installation`, `get_version`, `validate_target`, `prepare`, `build_command`, `execute`, `parse`, `normalize`, `cleanup`).
- `validators/`: three independent passes (structure, manifest, interface) aggregated by a `PluginValidator` facade; used by both the loader (fail-closed) and the `/validate` endpoint (report-only).
- `registry/`: in-memory `PluginRegistry` — duplicate-id rejection, enable/disable, lookup, inter-plugin dependency checking.
- `loader/`: `PluginLoader` discovers every subdirectory of the plugins root and loads it as a synthetic Python package (built via `importlib.util`/`ModuleSpec`, not real `__init__.py` files), so a plugin's `plugin.py` can use ordinary relative imports to reach its `parser.py`/`normalizer.py`/`validator.py` — and so this works regardless of where the configured plugins root actually lives on disk (verified against both the built-in directory and an arbitrary external directory). A broken plugin directory is recorded as a failure and never aborts discovery of the rest ("ignore invalid plugins, produce useful error messages").
- `manager/plugin_manager.py`: `PluginManager` facade (`list_plugins`, `get_plugin`, `reload_plugin`/`reload_all`, `validate_plugin`, `plugin_status`, `installed_plugins`, `check_dependencies`, `enable_plugin`/`disable_plugin`) plus a module-level singleton (`get_plugin_manager`), mirroring the DB engine's singleton pattern rather than `functools.lru_cache` since this is mutable in-memory state.
- `sdk/`: the curated import surface for plugin authors — `BasePlugin` + every model/exception, JSON/XML helpers (XML via `defusedxml`, not stdlib `xml.etree`, since plugins parse tool output that may reflect untrusted target content), file/temp helpers, a namespaced logger factory, and target validators that delegate to `backend.utils.target_validators` (no duplicated validation logic).
- `plugins/example_plugin/`: the one internal reference plugin, implementing every `BasePlugin` method with fixed, non-executing behavior. Never runs a subprocess, never produces a real finding, is never imported by application code — discovered purely by the framework scanning `backend/plugins/plugins/` at runtime.
- `README.md`: full plugin-authoring guide (lifecycle, layout, manifest spec, required methods, validation rules, how to add a real plugin).

**Backend API**: `GET /plugins`, `GET /plugins/{id}`, `GET /plugins/{id}/health`, `GET /plugins/{id}/validate`, `POST /plugins/reload` — via `backend/schemas/plugin.py` + `backend/services/plugin_service.py` (translates `PluginNotFoundError` → `NotFoundError` at the boundary, same pattern as `TargetService`) + `backend/api/routes/plugins.py`. Plugin discovery runs once at app startup (`main.py` lifespan) and logs a summary plus any per-plugin failure.

**Backend tests**: 14 new tests (35 total, up from 21) — API-level tests against the real `example-plugin`, plus framework-only unit tests (loader tolerating missing-files/malformed-manifest/missing-entrypoint directories, registry duplicate-id rejection, not-found, enable/disable, dependency checking, interface validator rejecting an incomplete subclass).

**Frontend**: `Tools` page rewritten into a real Plugin Management page — `PluginCard` component showing metadata, version, health (live-fetched, color-coded badge), enabled status, description, dependencies (with missing ones flagged), and validation status, backed by `usePlugins`/`usePlugin`/`usePluginHealth`/`useReloadPlugins` (TanStack Query) and `plugins-api.ts`. Shows an honest "No plugins installed" empty state when the registry is empty. `npm run build`/`npm run lint` both clean. **No browser automation tool was available this session either** (same limitation as Phase 3) — verified via build/type-check/lint and live network calls to the real backend, not a rendered screenshot.

**Explicitly not done in Phase 4** (per instructions): no Nmap/Nuclei/Nikto/WhatWeb/ffuf/Gobuster/Dirsearch or any other scanner, no subprocess execution, no task queue, no reports, no findings, no correlation. `execute()`/`build_command()` exist on the interface but nothing invokes them against a real tool yet.

## Phase 5 — Tool Management & Tool Configuration — Complete

Builds real detection for the 15 named tools (Nmap, Nuclei, WhatWeb, Nikto, HTTPX, Gobuster, Dirsearch, Feroxbuster, FFUF, SSLScan, Katana, Naabu, Subfinder, Amass, DNSx) on top of Phase 4's framework — still no execution, no scanning, no subprocess beyond a version-flag check.

- **Found and fixed a real bug in the Phase 4 loader**: a plugin's live instance and its `RegisteredPlugin` registry wrapper each got their own default `PluginConfiguration`, so a saved configuration change could never reach the running plugin. Fixed by constructing one `PluginConfiguration` and passing the same object to both (`backend/plugins/loader/plugin_loader.py`).
- `backend/plugins/models/config.py`: `PluginConfiguration` extended with `custom_executable_path`, `output_directory`, `http_proxy`/`https_proxy`/`socks_proxy`, `rate_limit`, `retries`, `wordlists` (slot name → path).
- `backend/plugins/sdk/detection_helpers.py`: `find_executable` (custom path → PATH → common install dirs, Windows and POSIX), `run_version_command` (the *only* subprocess call any of these plugins make — a fixed version flag only, never a target), `extract_version`, `validate_custom_executable`.
- `backend/plugins/sdk/detection_plugin.py`: `DetectionOnlyPlugin` base class shared by all 15 real plugins — implements `metadata()`, `check_installation()`/`get_version()`/`health()` (driven by three class attributes: `BINARY_NAMES`/`VERSION_ARGS`/`VERSION_PATTERN`), `prepare()`/`cleanup()` (no-ops), and `execute()` (always raises `PluginExecutionError` — execution is out of scope). Each concrete plugin only implements `validate_target()`, `build_command()` (real, tool-accurate argv construction — never executed), `parse()`, `normalize()`.
- 15 new plugin directories under `backend/plugins/plugins/` (90 files: `plugin.json`/`plugin.py`/`parser.py`/`normalizer.py`/`validator.py`/`README.md` each), all following the Phase 4 layout, all verified to discover/validate/load successfully.
- `backend/models/tool.py`: `Tool` gains `status`/`health_status`/`health_message`/`last_checked_at`; `ToolConfiguration` (already scaffolded in Phase 2, unused until now) gains `working_directory`/`custom_executable_path`/per-protocol proxies/`rate_limit`/`retries`/`output_directory`/`temp_directory` and drops the old generic `proxy` column. New `ToolStatus` (installed/missing/disabled/misconfigured/unsupported_version) and `ToolHealthStatus` (healthy/warning/error — a deliberately coarser vocabulary than the plugin framework's own 5-value `PluginHealthStatus`) enums. Migration `15d36d2a4f40`, autogenerated and applied; `downgrade()` on `tools` hits the same documented SQLite batch-mode limitation as the Phase 3 migration (verified, not fixed — `upgrade()` is the fully verified path).
- `backend/services/tool_service.py`: bridges the plugin framework (source of truth for what a tool *declares*) with the DB (source of truth for discovery *results* and user *configuration*) — `discover()` (re-scan + re-detect + persist all 15), `list_tools()`/`get_tool()` (search/filter/sort), `get_health()` (live, not cached), `validate_tools()` (one tool or all 15), `update_configuration()` (validates custom executable/wordlist paths, persists, and pushes the change onto the *same* live `PluginConfiguration` object the running plugin reads — no reload needed), `browse_filesystem()` (read-only directory listing backing the wordlist/path picker UI).
- 6 new REST endpoints: `GET /tools`, `GET /tools/{name}`, `POST /tools/discover`, `POST /tools/validate`, `PUT /tools/{name}/configuration`, `POST /tools/{name}/health`, plus `GET /tools/browse-filesystem` for the picker. Phase 4's generic `/plugins` API is untouched.
- 15 new backend tests (50 total, up from 35), written host-agnostically (a tool's real installed/not-installed state depends on the machine running the tests) — including an explicit no-fake-data invariant check across every returned tool.
- **Frontend**: `Tools` page rewritten again into the full Tool Management experience — search/status/health filters, sort, a `ToolCard` grid, a `ToolDetailsDialog` (Overview/Configuration/Diagnostics tabs) with a full configuration form (timeout, working/output/temp directories, custom executable path, proxies, rate limit, retries, arguments, environment variables, wordlists — each path field backed by a real server-side `FilesystemBrowseDialog`, since a browser can't reveal absolute server paths itself), and a professional `ToolsEmptyState` (per-tool install commands/links, supported tools list, Refresh Detection button) shown whenever nothing is installed yet. `npm run build`/`npm run lint` both clean; verified live through the Vite proxy against the real backend.

**Verified**: on the machine this was built on, only Nmap was actually installed — discovery correctly reported it as `installed`/`healthy` with its real detected version and path, and reported all other 14 tools as genuinely `missing` with no fabricated version/path/health data.

**Explicitly not done in Phase 5** (per instructions): no execution engine, no scan orchestration, no task queue, no findings, no reports. `execute()` exists on every plugin and always refuses.

## Phase 6 — Assessment Execution Engine — Complete

Builds the complete, tool-agnostic execution engine on top of Phase 4/5's plugin framework. No Nmap or any real scanner is integrated or executed — verified exclusively against a new `dummy-execution` test-fixture plugin (`backend/plugins/plugins/dummy_execution/`, never listed in Tool Management) that only sleeps, logs, and returns success/failure on demand.

- New `backend/workers/` package (planner, queue, manager, state, events, logger, exceptions) — a single bounded pool of `asyncio` workers (a semaphore, never a process pool/Celery/Redis, per this phase's explicit constraint), an `asyncio.PriorityQueue` for ordering/retry-priority/pre-start cancellation, an explicit job-state transition table (`state.py`), and an in-process event bus decoupling the dispatcher from structured per-job logging and the assessment activity log.
- `ExecutionPlanner`: one `ToolExecution` row (job) per (enabled target × requested tool), asking only each plugin's own `validate_target()` — never tool-specific logic. A pairing a plugin can't/won't run becomes a `SKIPPED` job, never a runnable one.
- `ExecutionManager`: the only place that calls a plugin's `prepare()`/`build_command()`/`execute()`/`cleanup()`; persists status/timestamps/output paths as a job progresses; implements cancel (queued or running), retry, and timeout. Reuses `Tool`/`ToolExecution` (Phase 2/5) — no new "Job" table.
- `backend/plugins/sdk/process_runner.py`: real, directly-tested `asyncio.create_subprocess_exec` execution (never `shell=True`) for a *future* tool-integration phase to call — not used by any plugin yet, since `dummy-execution` deliberately never starts an external program and every real tool's `execute()` still refuses.
- 7 new REST endpoints: `POST /assessments/{id}/execute`, `GET /assessments/{id}/progress`, `GET/POST /jobs`, `GET /jobs/{id}`, `GET /jobs/{id}/logs`, `POST /jobs/{id}/cancel`, `POST /jobs/{id}/retry`.
- Migration `a264a27eb29e`: adds `ToolExecution.created_at`/`retry_count`/`status_message` and the new job-lifecycle `ToolExecutionStatus` values.
- 16 new backend tests (66 total, up from 50) exercising the full engine end-to-end over the real HTTP API and a real SQLite DB: planning, concurrency, cancellation (both pre-start and mid-run), retry, timeout, failure (bad exit code and raised exception), live logging, progress reporting, and list/filter.
- **Three real concurrency bugs found and fixed while stress-testing this phase** (see `DECISIONS.md`): a cancellation race that could leave a job stuck at `queued`/`preparing` forever with no safety net; SQLite lock contention (`database is locked`) under the engine's now-genuine concurrent writers, fixed via a busy-timeout and switching to WAL journal mode; and a test-only race between a job's terminal status and the separate, later commit that reverts its assessment's status, fixed by polling for the assessment status in the affected tests instead of reading it once.
- **Frontend**: new `Executions` page — assessment picker, live progress panel, running/queued and history job tables (1.5s polling via TanStack Query, matching every other "live" view in this app), an `ExecuteDialog` (tool/target multi-select), a `JobDetailsDialog` (cancel/retry buttons, gated by the same status sets the backend enforces) with a professional `LogViewer` (monospace, auto-scroll, search, copy, download). `npm run build`/`npm run lint` both clean.
- **Verified live** against the real dev database (not just pytest): created a real assessment/target, ran `dummy-execution` to completion with real captured logs, cancelled a genuinely in-flight job (confirmed via `status: "running"` → `"cancelled"` with `duration` recorded), confirmed the assessment status correctly reverted, retried the cancelled job to a real completion, and confirmed the 409 guard rejecting a cancel on an already-terminal job — then cleaned up the demo assessment. **No browser automation tool was available this session either** (same limitation as every prior phase) — the Executions page was verified via build/type-check/lint and this live API walkthrough, not a rendered screenshot.

**Explicitly not done in Phase 6** (per instructions): no Nmap integration, no real security tool executed, no pause/resume of an in-flight OS process (not portably possible on Windows without unsupported thread-suspension tricks — cancel/kill/retry/timeout are the real, implemented process-control primitives), no findings, no reports, no correlation.

## Phase 7 — Nmap Integration with Scan Profile Engine — Complete

Rebuilds the Nmap plugin as the reference implementation for every future scanner plugin — real execution (the first plugin whose `execute()` doesn't unconditionally refuse), driven by reusable, data-only "Scan Profiles" rather than hardcoded commands, with real XML parsing and normalization into generic (tool-agnostic) Assets/Services/Observations — explicitly never Findings/CVSS/vulnerabilities.

- New `assets`/`services`/`observations` tables (generic, not Nmap-specific — any future scanner plugin populates the same three) plus `tool_executions.profile_id`/`generated_command`/`advanced_options`. New `AssetState`/`NetworkProtocol`/`PortState` enums. Migration `63098bf45bce`.
- `backend/plugins/models/normalized.py`: the common `NormalizedOutput`/`NormalizedAsset`/`NormalizedService`/`NormalizedObservation` shape any scanner plugin's `normalize()` returns.
- Full Nmap plugin rewrite (`backend/plugins/plugins/nmap/`): `models.py` (`ScanProfile`/`ProfileCategory`/`RiskLevel`/`AdvancedOptions`), `profile_manager.py` (load/validate/CRUD; built-in profiles read-only, custom ones on disk), `command_builder.py` (the one place profile+target+options become an argv, zero per-profile branching), real `parser.py` (defusedxml) + `normalizer.py`, `plugin.py` (overrides `execute()` for real via `run_subprocess`). 31 built-in profiles.
- `backend/workers/planner.py`/`manager.py` wired for per-tool `ToolExecutionOptions` (profile_id/advanced_options); a successful run parses/normalizes/persists generically.
- New `/tools/{tool_name}/profiles` API (full CRUD + preview-command/import/export/duplicate), `ExecuteRequest.tool_options`, `GET /jobs/{id}/results` and `GET /jobs/{id}/raw-output`.
- **Real bug found & fixed**: a plugin's synthetically-loaded module (Phase 4's loader design) gives its exception classes a distinct identity from the same dotted import path used outside the plugin — code catching a plugin's own exception type from outside it silently never matches. Fixed by having plugin-specific errors inherit from the plugin framework's stable base exceptions, and having external callers pass plain dicts instead of constructing the plugin's own Pydantic types.
- 47 new backend tests (113 total, up from 66), all passing across repeated stress reruns.
- **Frontend** (built in a later session than the backend): profile selection wired into `ExecuteDialog` (defaults to `service_detection`), a Results/Log tabbed view in `JobDetailsDialog` (new `JobResultsView.tsx` — hosts/ports/services tables) with a "Download XML" button. Verified through the real browser UI (Playwright) end-to-end: choose profile → run → live progress → completed → structured results → real XML download.
- A dedicated `/tools/nmap` scan-console page (category browsing, advanced options, live command preview) and a custom Scan Profile management UI were scoped but not built — the golden path works entirely through the generic Executions page.

**Explicitly not done in Phase 7**: no Nuclei/other scanner integration, no correlation, no severity/CVSS anywhere in the new model.

## Phase 8 — Asset Inventory & Observation Engine — Complete

Turns Phase 7's per-job Asset/Service/Observation snapshot into the durable, deduplicated inventory every future scanner plugin normalizes into. Phase 7's `Asset.execution_id` (`nullable=False`) meant every scan run created brand-new rows with zero lookup against prior scans — re-scanning `127.0.0.1` ten times produced ten separate `Asset` rows, only the single best Nmap OS guess was kept (all other candidates discarded), and only the first IPv4/first IPv6 address was kept (dual-stack hosts lost data).

**New domain model** (9 new/restructured tables, `backend/models/`): `Asset` (restructured — `assessment_id` scoping replacing direct `execution_id`, `ipv4`/`ipv6`/`fqdn` split from one `ip_address`, `asset_type`, `fingerprint` merge key, `first_seen`/`last_seen`), `NetworkInterface` (every address, not just the first), `Service` (restructured — `vendor`/`banner`/`fingerprint`/`first_seen`/`last_seen`), `Technology` (extracted from `Service.product`/`.version`), `OperatingSystem` (every OS candidate, not just the best), `Observation` (restructured — `service_id`/`plugin`/`category`/`observation_type`/`fingerprint`; deliberately no severity/confidence), `ObservationEvidence` (append-only, mirrors the existing `FindingEvidence` pattern), `Fingerprint` (protocol-level identity evidence — SSH/TLS/HTTP/SMB/banner/hash — distinct from the merge-key columns), `ExecutionAsset`/`ExecutionObservation` (join tables recording which execution touched which asset/observation, `is_new` distinguishing discovery from re-confirmation). 4 new enums (`AssetType`/`TechnologyCategory`/`ObservationCategory`/`FingerprintType`).

- `backend/services/fingerprinting.py`: pure, deterministic merge-key functions (`asset_fingerprint` — MAC > IPv4/IPv6 > hostname priority; `service_fingerprint`; `observation_fingerprint`), each a namespaced sha256 digest so different identity *kinds* can never collide.
- `backend/services/asset_inventory_service.py`: the merge/upsert engine — the only place a `NormalizedOutput` becomes persisted inventory rows, replacing `ExecutionManager`'s old inline logic. Find-or-insert-or-update per asset/service/technology/OS/observation, keeping the workers package tool-agnostic-dispatch-only.
- `backend/plugins/models/normalized.py` + the Nmap normalizer extended (additively) to pass through *every* OS candidate and *every* address instead of only the best/first — a genuine data-loss fix, verified live (10 OS candidates captured on this dev machine where only 1 survived before).
- One real Alembic migration with actual production data on the line (6 assets/47 services/2 observations from prior sessions' testing) — staged in phases (additive nullable columns → Python-driven backfill computing real fingerprints via the same functions the app uses → tighten to NOT NULL/drop old columns), verified with zero `PRAGMA foreign_key_check` violations and a row-count comparison proving no scans were lost.
- **Real bug found & fixed during downgrade testing**: `Enum(create_constraint=True)`'s CHECK constraint keeps getting re-emitted by Alembic's SQLite batch-mode table-recreate even after an explicit `drop_constraint()` — a narrow, known Alembic/SQLite interaction. Worked around by leaving two enum columns (`asset_type`/`category`) undropped on downgrade (harmless, documented), matching this project's established "upgrade is the fully verified path" precedent from Phases 3/5/7.
- **Real regression found & fixed**: `ExecutionService.get_results` (`GET /jobs/{id}/results`, used by the Phase 7 frontend's `JobResultsView`) queried `Asset.execution_id`, a column Phase 8 removed entirely — caught via live re-verification against a real scan (not assumed safe), fixed to join through `ExecutionAsset` and derive OS name/accuracy from the asset's best current `OperatingSystem` candidate instead of two now-nonexistent columns.
- 5 new read-only API resources (`GET /assets`, `/assets/{id}`, `/services`, `/technologies`, `/observations`, `/operating-systems`, all with search/filter/sort/pagination) plus `GET /search` (unions matches across assets/services/technologies/observations). One consolidated `AssetInventoryQueryService` rather than five near-duplicate services.
- 12 new backend tests (125 total, up from 113) — fingerprint determinism unit tests plus real end-to-end integration tests against actual installed Nmap: single scan, **re-scan the same target** (same `Asset.id`, no duplicate `Service`/`Technology` rows, `last_seen` advances, execution history grows to 2 entries with correct `is_new` flags), observations, search. All passing across 5 consecutive full-suite stress reruns.
- **Frontend**: 5 new list pages (Assets/Services/Technologies/Observations/Operating Systems) plus an Asset Details page (7 tabs: Overview/Network Interfaces/Services/Technologies/Operating System/Observations-with-inline-evidence/Execution History) and a Search page — wired the previously 100%-decorative "Search workspace" header button to a real page instead of a ⌘K command palette (no new dependency). `npm run build`/`npm run lint` both clean.
- **Verified live through the real browser UI** (Playwright): confirmed the 6 pre-existing migrated assets still render correctly, ran a real Nmap OS-detection scan (10 OS candidates captured, all visible in the UI), re-ran a second scan against the same target, and watched the Asset Details Execution History tab correctly show both "Discovered" and "Re-confirmed" entries for the same asset — no duplicate row created.

**Explicitly not done in Phase 8** (per instructions): no Nuclei integration, no correlation engine, no `Finding` rows created or touched (Phase 2's `Finding`/`FindingEvidence`/`FindingReference` remain completely untouched, reserved for a future phase), no severity/CVSS anywhere in the new model, no ⌘K command palette.

## Phase 9 — Correlation Engine & Intelligence Dashboard — Complete

Builds the Correlation Engine (turns Phase 6-8's frozen, real inventory into `Finding` rows via deterministic rules) and rebuilds the Dashboard into a real intelligence dashboard — the first phase to touch Phase 2's `Finding`/`FindingEvidence`/`FindingReference` tables since they were scaffolded. Per this phase's explicit constraint, `backend/workers/` (the execution engine) and the Nmap plugin were never touched; the Correlation Engine is its own, separately-triggered pipeline stage.

**Schema** (migration `f3a9c1d5e7b2`): `findings` was empty in every environment through Phase 8 (verified — `SELECT COUNT(*) FROM findings` = 0 on the real dev DB before writing the migration, with a runtime guard that aborts `upgrade()` if it ever isn't), so this is a plain drop-and-recreate rather than Phase 8's staged additive/backfill/tighten dance. `Finding` restructured: `execution_id` (`NOT NULL`, `CASCADE`) replaced by `assessment_id`/`asset_id` (the actual ownership) and `source_execution_id` (nullable, `SET NULL`, "most recently re-confirmed by" — mirrors `Observation.execution_id`); added `rule_id`, `plugin`, `impact`, `fingerprint`, `first_seen`/`last_seen`. New `finding_observations` (join table, mirrors `ExecutionObservation`) and `correlation_runs` (durable history of every engine pass, backing `GET /correlation/status`). `FindingEvidence`/`FindingReference` schemas unchanged, just recreated. Verified against the real dev DB: zero `PRAGMA foreign_key_check` violations, all pre-existing 10 assets/83 services/20 technologies/2 observations intact.

**Correlation Engine** (`backend/correlation/`, new top-level package parallel to `backend/plugins`/`backend/workers`): `base.py` (`CorrelationRule` interface: `rule_id`/`title`/`description`/`category`/`severity`/`base_confidence`/`impact`/`remediation`/`references` class attributes + one `evaluate(RuleContext) -> list[FindingCandidate]` method), `models.py` (pure `RuleContext`/`FindingCandidate`/`RuleReference` dataclasses, no DB session), `registry.py` (static in-memory catalog, duplicate-id rejection), `text_utils.py` (keyword matching for NSE-script-output rules). `rules/`: 23 real rules across all 10 required categories (service/technology/os/protocol/configuration/tls/ssh/smb/http/general) — e.g. `SVC-001` Telnet exposed, `SVC-005` database/search service exposed, `TECH-001` end-of-life software, `OS-001` end-of-life OS (best-accuracy candidate only, `>=85%` threshold), `SSH-001` weak SSH algorithms (the phase brief's own worked example), `SMB-001`/`SMB-002` signing/SMBv1, `TLS-001`/`002`/`003` self-signed/expired/weak-cipher, `CONFIG-001` anonymous FTP, `HTTP-001`/`002`/`003` missing headers/directory listing, `GEN-001` version disclosure. Every reference is a real, static CWE/OWASP id (`RuleReference`) — no rule ever attaches a CVE or a CVSS score, since neither can be determined deterministically from collected data without inventing one.

**`backend/services/correlation_service.py`** (write side, mirrors `AssetInventoryService`'s role): per asset, builds a `RuleContext` from its current `Service`/`Technology`/`OperatingSystem`/`Observation` rows, evaluates every registered rule, and — critically — merges *all* candidates one rule returns for one asset into **one** `Finding` (fingerprint = `rule_id` + `asset.fingerprint`, so a rule fires at most once per asset; re-running is idempotent, verified directly). Confidence starts at the rule's `base_confidence` and is bumped up the LOW→MEDIUM→HIGH→CONFIRMED ladder once for `>=2` supporting observations and once more for `>=2` distinct confirming executions (falling back to `ExecutionAsset` reconfirmation count when a rule has no observations to count, e.g. pure service/tech/OS rules) — verified with a real two-execution reconfirmation test. `plugin` is resolved once per asset via a real join through `source_execution_id -> Tool.name`, never hardcoded. Explicitly **not** wired into `backend/workers/manager.py` — the frozen execution engine is never modified; correlation is triggered explicitly via `POST /correlation/run` (per-assessment or global), recorded as a `CorrelationRun` history row.

**Read APIs**: `backend/services/finding_query_service.py` (`GET /findings` search/filter/sort/paginate by assessment/asset/severity/confidence/status/category/plugin/rule_id; `GET /findings/{id}` full detail: evidence, references, supporting observations, the asset's current services as "affected services", and execution history derived from the asset's own `ExecutionAsset` log) and `backend/services/dashboard_service.py` (`GET /dashboard`, `GET /statistics` — every number a real aggregated `GROUP BY`/`COUNT`/`AVG` query, optionally scoped to one assessment or workspace-wide; overview, security summary, execution summary, asset summary, finding dashboard incl. newest/by-category/by-plugin/by-asset/open-vs-resolved, asset dashboard incl. newest/most-active/OS/tech/service distribution, and 4 trend series over the last 30 days). `backend/api/routes/correlation.py` (`POST /correlation/run`, `GET /correlation/status`). Global search (`AssetInventoryQueryService.search`) extended to include findings.

**Real bug found and fixed during Playwright verification**: `DistributionBarChart`'s per-bar `<Cell key={row.label}>` collided whenever two categories legitimately shared a display label — e.g. "Findings by Asset" showing 10 different `localhost` assets (Asset identity is scoped per-assessment since Phase 8, so the same hostname recurs across assessments) all rendered as duplicate React keys, a real console warning. Fixed two ways: the `Cell` key now includes the array index, and `DashboardService._disambiguate()` appends a short id suffix (`localhost (055a25d7)`) to any label that collides with another in the same result set — both a chart-correctness fix and a real UX ambiguity fix, not merely cosmetic.

**Real bug found and fixed during Playwright verification (2)**: `TrendAreaChart`'s `margin={{ left: -20 }}` (added to visually tighten the chart) clipped the Y-axis tick labels down to unreadable fragments that looked like a repeated "0"/"3". Fixed by removing the negative margin and widening the axis gutter instead — a screenshot-driven catch, not something `tsc`/`eslint`/unit tests could have caught, consistent with this project's established practice of real browser verification via Playwright (no `chromium-cli` available in this environment).

**Backend tests**: 13 new tests (138 total, up from 125) — rule unit tests (pure, no DB: telnet/SMB/SSH-weak-algorithm matching, including a true-negative case), fingerprint determinism, and real integration tests seeding an assessment/asset/service/observation directly via the DB session (no Nmap dependency, unlike Phase 8's asset-inventory tests) covering finding creation, idempotent re-correlation (zero duplicates), a no-match case, confidence escalation across two distinct executions, the full `POST /correlation/run` -> `GET /findings` -> `GET /findings/{id}` -> `GET /dashboard` -> `GET /statistics` HTTP path, and the dashboard's empty-state contract for an assessment with zero data. All 138 passing.

**Frontend**: `recharts` v3 added (React 18-compatible; v2 is deprecated upstream). A validated dark-mode-only chart palette (`utils/chart-palette.ts` — this app forces dark mode unconditionally, see `components/theme-provider.tsx`, so only the dark categorical/status steps of the dataviz skill's reference palette are used, checked with its `validate_palette.js` rather than eyeballed) backs three reusable chart primitives (`ChartCard`, `DistributionBarChart`, `TrendAreaChart`) plus `StatTile` and `SeverityBadge` (icon + label, never color-alone, per the status-palette rule). Rebuilt `Dashboard.tsx` into a real intelligence dashboard (overview/security/execution summaries, 4 trend charts, finding + asset dashboards, an assessment-scope picker, a "Run Correlation" action) plus five new pages: `Findings` (filter/search/paginate), `FindingDetails` (6 tabs: Overview/Evidence/Observations/Affected Services/References/Execution History), `SecurityOverview`, `AssetOverview`, `TechnologyOverview`. `npm run build`/`npm run lint` both clean.

**Verified live** through the real browser UI (Playwright, screenshots reviewed): ran the Correlation Engine against this machine's real, pre-existing dev-database scan data (10 assets, 83 services, 20 technologies, 2 observations from prior phases' live testing) — **40 real findings generated** on the first run (0 fabricated, every one traced to a real open service, technology, or NSE observation), confirmed fully idempotent on a second run (0 created, 40 re-confirmed, confidence correctly escalated to `CONFIRMED` for assets re-scanned across multiple executions), and confirmed every new page renders this real data correctly with zero console errors after the two bugs above were fixed. Also verified an empty assessment renders the dashboard's honest empty state (`is_empty: true`, zeroed counts, no fabricated placeholder charts).

**Explicitly not done in Phase 9** (per instructions): no execution-engine or Nmap-plugin changes, no additional scanner integrations (Nuclei is explicitly the next phase), no fabricated CVEs or CVSS scores anywhere in any of the 23 rules, no finding-triage/status-update endpoints (the phase brief's API list was GET-only plus the one necessary `POST /correlation/run` trigger).

## Architectural Change — Linux-Only Runtime

Not a feature phase — a project-direction change applied between Phase 9 and Phase 10 (Nuclei). The application's runtime target is now **Linux only**; Windows is development-only (this repo continues to be edited on Windows via Claude Code, then copied to a Linux machine to run/test after every phase).

**Code audited and changed**: a repo-wide search for Windows-specific runtime logic (`platform.system() == "Windows"`, `.exe` suffixes, `Program Files`/registry/`AppData` lookups, `shell=True`/`cmd.exe`/PowerShell subprocess invocation, hardcoded backslash paths) found exactly one real Windows code path in the entire backend: `backend/plugins/sdk/detection_helpers.py`'s `default_search_directories()`/`find_executable()`, which branched on `platform.system()` to search Windows install locations (`Program Files`, Chocolatey, Scoop, WinGet) and appended a `.exe` suffix. Removed — detection now only ever searches `PATH` (`shutil.which`) plus Linux install directories (`~/go/bin`, `~/.local/bin`, `/usr/local/bin`, `/usr/local/sbin`, `/usr/bin`, `/usr/sbin`, `/opt`, `/snap/bin`). Everywhere else (`core/config.py`, `plugins/sdk/file_helpers.py`, `plugins/sdk/temp_helpers.py`, `plugins/utils/plugin_paths.py`, both Dockerfiles, the frontend) was already pure `pathlib`/POSIX-compatible with no changes needed — a direct result of this project's pre-existing "no hardcoded paths" rule (`.claude/CLAUDE.md`) having been followed from Phase 1 onward. Full backend suite re-run after the change: 153/153 passing.

**Added**: real Linux launcher scripts — `backend/run_backend.sh`, `frontend/run_frontend.sh`, `run_all.sh` (repo root) — mirroring the existing Windows `.ps1`/`.bat` scripts' behavior (create venv/install deps/apply migrations/start server; install node_modules/start Vite) using Bash/POSIX conventions (`set -euo pipefail`, `command -v`, background processes with `nohup`/PIDs instead of separate console windows, since Linux has no equivalent of `Start-Process`/`cmd /k` opening new windows). The Windows scripts are kept as-is (developer-convenience only, per `.claude/CLAUDE.md`'s runtime-platform rules) since the dev machine is still Windows.

**Added**: `docs/LINUX_TOOL_INSTALLATION.md` — apt/dnf/pacman/upstream install instructions for every currently-integrated tool (nmap, plus the 15 detection-only plugins: nuclei, nikto, whatweb, subfinder, sslscan, naabu, katana, httpx, gobuster, ffuf, feroxbuster, dnsx, dirsearch, amass), satisfying the standing rule that a newly integrated tool's Linux installation steps get documented as part of that integration.

**Documentation updated**: `.claude/CLAUDE.md` (new "Runtime Platform" section — Linux-only rules), `.claude/MEMORY.md` (new "Runtime Platform" section + updated "Current Phase"), `.claude/PROJECT_STRUCTURE.md` (notes the new `docs/LINUX_TOOL_INSTALLATION.md` and the `.sh`/`.ps1`/`.bat` script split), this file, and `DECISIONS.md`.

## Phase 10 — Tool Management 2.0 (Production Plugin Management) — Complete

Upgrades the plugin/tool detection and management system into production-quality infrastructure every future scanner plugin (Nuclei included) rides on for free. Per this phase's explicit constraint, no new scanner tool was integrated — Nmap remains the only tool with real execution; the other 15 remain detection-only.

**Plugin metadata**: `PluginManifest`/`PluginMetadata` gained `minimum_tool_version`/`recommended_tool_version` (nullable, informational — populated only for Nmap's manifest, `"7.00"`, since asserting a number for tools never version-tested here would be a fabricated claim, not a documented fact). "Supported Scan Profiles" is exposed as a computed `supports_profiles: bool` on `ToolDetail` (does the plugin have a `profile_manager`) rather than a static manifest field, since it's a structural fact about the plugin class, not a declared capability that could drift from reality.

**Tolerant version detection** (`backend/plugins/sdk/detection_helpers.py`): new `detect_version()` + `VersionDetectionResult` try a plugin's own preferred version flag first, then fall back through `-version`/`version`/`-v`/`-V` if that yields no output, and match a plugin's own `VERSION_PATTERN` first before falling back to a generic dotted-version regex (`_GENERIC_VERSION_PATTERN`) if that doesn't match either. `DetectionOnlyPlugin.get_version()`/`diagnostics()` both go through it — one implementation, not fifteen. New `resolve_executable_detailed()` returns not just the resolved path but *how* it was found (`custom_path`/`path`/`search_directory`/`not_found`), the detection-method fact every other part of this phase surfaces. New `is_version_at_least()`/`parse_version_tuple()` do numeric (not lexical) dotted-version comparison, used by validation's minimum-version check.

**Diagnostics**: new `backend/plugins/models/diagnostics.py` (`PluginDiagnostics`) + `DetectionOnlyPlugin.diagnostics()` — one shared implementation (via a new `_build_health()` helper factored out of `health()` so detection/version run exactly once, not up to three times across `resolve_executable()`/`get_version()`/`health()`/`diagnostics()`) covering every tool plugin, Nmap included (it extends `DetectionOnlyPlugin` too). `ToolService.get_diagnostics()` adds the HTTP-facing extras: real `PATH` directory listing, "relevant only" environment variables (`PATH`/`HOME` plus whatever the tool's own configuration sets: env vars, HTTP/HTTPS/SOCKS proxy), and the current validation errors/warnings. New endpoint `GET /tools/{name}/diagnostics`.

**Health checks / unified status**: new `ToolOverallStatus` enum (`healthy`/`warning`/`error`/`disabled`/`unsupported_version`/`missing` — exactly Tool Management's required six states) and a pure `derive_overall_status()` function collapsing the pre-existing two independent dimensions (`ToolStatus` lifecycle + `ToolHealthStatus` health) into the one badge a user actually wants; `MISCONFIGURED` (a `ToolStatus` value with no slot in the unified vocabulary) maps to `error`. Added to `ToolSummary`/`ToolDetail` as `overall_status`.

**Validation**: `_validate_one` now also checks a detected version against the manifest's `minimum_tool_version` (via `is_version_at_least`) and warns (never blocks) if older. New `Tool.last_validated_at` column, stamped only by an explicit validate action (`POST /tools/{name}/validate` or the bulk `POST /tools/validate`) — deliberately NOT by `GET /tools/{name}`, which computes validation live for display without mutating that timestamp (a real distinction verified by a dedicated test, since the two code paths look almost identical).

**Profile management generalized**: `GET /tools/{name}/profiles` (list) now returns an empty list for a tool with no profile system instead of a 422 — the Profiles tab needs "no profiles" to be a normal empty state for 14 of 15 tools, not an error surfaced to the UI; every *mutating* profile endpoint still 422s cleanly. New per-profile enable/disable: `POST /tools/{name}/profiles/{id}/enable|disable`, backed by a new `PluginConfiguration.disabled_profile_ids` (in-memory, pushed immediately, no reload needed) persisted via a new `ToolConfiguration.disabled_profiles_json` column — works uniformly for built-in and custom profiles since disabling never edits/deletes the profile itself, just excludes it from being offered. `ScanProfileRead` gained `enabled: bool`.

**Real bug found and fixed**: `ToolService._sync_catalog()` ensured a `ToolConfiguration` DB row existed for every tool but never pushed it onto the live, in-memory `PluginConfiguration` the running plugin actually reads (`_apply_to_live_config` was previously only called from `update_configuration`). This meant *any* persisted tool configuration (custom executable path, proxy, timeout, wordlists — not just this phase's new `disabled_profile_ids`) silently stopped taking effect the moment the backend process restarted, until a user re-submitted the same settings again through the Settings tab, even though the database still showed it as configured. Fixed by calling `_apply_to_live_config` during `_sync_catalog` too (which runs on every `discover()`/app-lifecycle sync pass), so persisted configuration actually persists across restarts, not just across the lifetime of one process.

**New API** (all under `ToolServiceDep`/`ScanProfileServiceDep`, wired through existing dependency plumbing): `GET /tools/{name}/diagnostics`, `POST /tools/{name}/refresh` (re-run detection/version/health for one tool — lighter than the full `POST /tools/discover` re-scan), `POST /tools/{name}/validate` (per-tool alias of the existing bulk validate, same "report invalid rather than 404" contract for consistency with a batch request never failing outright over one bad name), `POST /tools/{name}/profiles/{id}/enable`, `POST /tools/{name}/profiles/{id}/disable`.

**Manifests**: all 15 supported tools except `dirsearch` (deliberately left blank, preserving a real "manifest omits the field" test case) gained real Linux `install_instructions` (apt/dnf/pacman/AUR/official-method guidance, sourced from the same research already written up in `docs/LINUX_TOOL_INSTALLATION.md`), replacing Nuclei's stale Windows/macOS/Linux tri-platform entry (written before the Linux-only architecture decision) with a Linux-only one.

**Migration** `d12d16aab9d9`: two additive nullable columns (`tools.last_validated_at`, `tool_configurations.disabled_profiles_json`), applied against the real dev database (backed up first: `data/app.db.bak-phase10-20260721120041`), verified with zero `PRAGMA foreign_key_check` violations.

**Backend tests**: 15 new tests (`backend/tests/test_tool_management_v2.py`) covering pure version-comparison helpers (no subprocess dependency), diagnostics structure/never-fabricates/404, per-tool refresh/validate (including the shared-test-DB-safe relative-timestamp assertions, since this suite's DB persists across the whole session by design), overall-status derivation, and the profile enable/disable round-trip including the "no profile support" 422 case. Two pre-existing tests updated for the manifest-normalization decision above. Full deterministic suite (169 tests, excluding host/nmap tests gated behind real installed-nmap execution against `127.0.0.1`, which are flaky on this Windows dev machine for privilege reasons unrelated to this phase) passing.

**Frontend**: `ToolDetailsDialog`/`ToolStatusBadges` (modal-based, two separate status badges) deleted, superseded by a routed `tools/:name` page (`ToolDetails.tsx`, following the same header-card + shadcn-`Tabs` convention as `FindingDetails.tsx`) with four tabs — Overview (now also showing detected/minimum tool version, detection method, Scan Profile support, last validation), Diagnostics (`ToolDiagnosticsPanel.tsx` — real resolved path/detection method/version command/raw version output/health output/PATH/environment variables/validation, each live-fetched from the new endpoint), Profiles (`ToolProfilesPanel.tsx` — search/category/risk filters, per-profile enable/disable `Switch`, duplicate/export/import dialogs, an inline expandable detail row; a real "No Scan Profiles for this tool" empty state for the 14 tools without one), and Settings (the pre-existing `ToolConfigurationForm`, unchanged). New unified `ToolOverallStatusBadge` (icon + label, never color alone) replaces the two separate badges everywhere, including `ToolCard`. `tsc -b`, `eslint`, and `vite build` all clean.

**Real bug found and fixed during Playwright verification**: `ToolDiagnosticsPanel`'s PATH-directory list rendered `<Badge key={dir}>` — this dev machine's real `PATH` contains genuine duplicate directories (e.g. `C:\Program Files\Git\usr\bin` twice), a live React duplicate-key warning caught only by rendering real diagnostics data, not by `tsc`/`eslint`. Fixed the same way this project fixed an identical class of bug in Phase 9's `DistributionBarChart` (`DECISIONS.md`): qualify the key with its array index.

**Verified live** through the real browser UI (Playwright, screenshots reviewed) against this machine's real, already-installed Nmap (v7.97): Tool Management's list shows the new unified status badges for all 15 tools (Nmap "Healthy", the other 14 "Missing" — genuinely not installed here, not fabricated); Nmap's Tool Details page shows real diagnostics (actual `nmap --version` banner, actual resolved path `C:\Program Files (x86)\Nmap\nmap.EXE`, actual PATH) and its 31 real built-in Scan Profiles with working enable/disable toggles (round-tripped live: checked→unchecked→checked, persisted through the backend each time); Nikto's (no profile system) Profiles tab shows the real empty state, and its "not installed" banner shows the new real Linux install guidance. Zero console errors, zero 4xx/5xx after the PATH-key fix above.

**Explicitly not done in Phase 10** (per instructions): no new scanner tool integrated, no per-tool execution concurrency limiting (the execution engine's concurrency is a single process-wide `asyncio.Semaphore`, per Phase 6 — adding a per-tool "Concurrency" setting with no code path that actually enforces it would be a decorative, half-implemented control, which this project's own rules forbid; deferred, not silently dropped, until/unless the execution engine gains per-tool queuing), no recommended_tool_version guesses for tools never version-tested against this codebase, no deletion of custom Scan Profiles from the new UI (the API supports it; the phase brief's UI list was View/Duplicate/Export/Import/Enable/Disable, not Delete).

**Explicitly not done**: no new features, no Phase 10/Nuclei work started, no removal of the Windows developer-convenience scripts (still needed since development happens on Windows), no rewrite of already-correct cross-platform code.

## Phase 11 — Nikto & Nuclei Integration — Complete

Upgrades Nikto and Nuclei from Phase 5's detection-only stubs into the second and third plugins (after Nmap) with real execution, following Nmap's reference architecture exactly — `plugin.py`/`models.py`/`command_builder.py`/`profile_manager.py`/`parser.py`/`normalizer.py`/`validator.py`/`README.md`/`profiles/*.json` for each, zero shared execution/parsing code duplicated (both reuse the same `run_subprocess` SDK helper, the same `NormalizedOutput` shape, the same `ProfileManager`/`ScanProfile` pattern-by-convention).

**Nikto** (`backend/plugins/plugins/nikto/`): real `-Format xml -o -` execution via `run_subprocess`. New `models.py` (`ScanProfile` with `tuning`/`plugins`/`timeout_seconds` — Nikto's own `-Tuning`/`-Plugins`/`-timeout` flags, not Nmap's ports/scripts). Nine built-in profiles matching the phase brief exactly: Quick Scan, Default Scan (the recommended profile), Full Scan, SSL Scan, Headers, Interesting Files, CGI, Misconfiguration Scan, Custom Scan. New `parser.py` (real `NiktoFinding`/`NiktoHost` dataclasses from Nikto's XML `<scandetails>`/`<item>` elements — id/osvdbid/method/description/uri) and `normalizer.py` (real CVE/CWE extraction via regex over Nikto's own finding text, folded into `Observation.detail` as plain facts — never a fabricated Finding). `validator.py` gained `resolve_nikto_target()` (URL -> host/port/`-ssl` facts, mirroring Nmap's `resolve_nmap_target`).

**Nuclei** (`backend/plugins/plugins/nuclei/`): real `-jsonl -silent` execution. New `models.py` (`ScanProfile` with `templates`/`tags`/`exclude_tags`/`severities` — Nuclei's own template-selection dimensions). Nine built-in profiles: Quick, Default (the recommended profile — CVEs/vulnerabilities/exposures/misconfiguration/technologies/default-logins), Web, SSL, Exposure, CVE, Misconfiguration, Technology, Custom Templates. New `parser.py` (real `NucleiFinding` dataclass from each JSONL result line — template-id/severity/classification.cve-id/cwe-id/cvss-score/matched-at/matcher-name/extracted-results, malformed lines skipped not fatal) and `normalizer.py` (Nuclei's own real severity/CVE/CWE/CVSS — already structured in its output, unlike Nikto's free text — folded into `Observation.detail` verbatim, still never a fabricated Finding of its own).

**Real, generalizable architecture bug found and fixed**: `ScanProfileRead`/`ScanProfileWrite`/`ScanProfileService._to_read` were hardcoded to Nmap's own field names (`required_ports`/`required_scripts`/`script_args`) from when Nmap was the only plugin with a profile system. Serving Nikto's or Nuclei's own, differently-shaped `ScanProfile` over the same shared `/tools/{name}/profiles` endpoint raised a real 500 (`AttributeError: 'ScanProfile' object has no attribute 'required_ports'`) — caught only by calling the real HTTP endpoint after Tool Management already showed "supports_profiles: true" for both (a plugin-level unit test calling `ProfileManager` directly never touches this HTTP-facing schema layer at all). Fixed by making `ScanProfileRead`/`ScanProfileWrite` a superset schema (every tool's own fields, all optional/defaulted) and `_to_read` read each tool-specific field via `getattr(profile, name, default)` rather than direct attribute access. Also renamed `ScanProfile.minimum_nmap_version` -> `minimum_tool_version` project-wide (models, schemas, service, all 29 Nmap profile JSON files, frontend types/components, tests) since that field name being Nmap-specific was itself already a smaller instance of the same problem, caught while designing Nikto/Nuclei's own profile models.

**Correlation Engine**: new `backend/correlation/rules/cross_tool_rules.py` — the first rule module to branch on `Observation.plugin` (the originating tool, already denormalized onto every row since Phase 8/9) specifically to combine signals from *different* tools into one Finding, per the phase brief's own worked example (Nmap fingerprints Apache 2.4.49 -> Nikto observes the same version in its own header check -> Nuclei's live template match confirms CVE-2021-41773 -> **one** Finding, evidence linked from all three). Two rules: `MultiToolConfirmedCveRule` (`CROSS-001`, fires whenever the *same* CVE id appears verbatim in real output text from 2+ distinct tools — no static table needed) and `KnownVulnerableTechnologyConfirmedByTemplateRule` (`CROSS-002`, the literal worked example — a small, honest product/version -> CVE table used only to *explain* the match, never to cite a CVE unless Nuclei's own live template match already, independently confirmed that exact CVE against the same host; a version match alone never fires it). Verified end-to-end against a real SQLite database (no mocking): seeded an Nmap `Technology` row + Nikto/Nuclei `Observation` rows exactly as `HostInventoryService` would produce them, ran the real `CorrelationService`, got exactly one `CROSS-002` Finding with `FindingObservation` links spanning both `nikto` and `nuclei`.

**Real bug found and fixed (pre-existing, surfaced by this phase)**: `FindingCandidate.title_override` was declared on the dataclass but never read anywhere in `correlation_service.py`'s `_upsert_finding` -- every Finding's title silently always came from the rule's static `title` class attribute, no matter what a candidate set. Harmless until now because no existing rule ever set it; `cross_tool_rules.py` is the first to want a match-specific title (e.g. naming the actual CVE). Fixed by using the first candidate's `title_override` (if any) when a Finding is first created — consistent with `description`/`impact` also only being set once, at creation, never per-candidate.

**Correlation rule quality bar maintained**: neither new rule ever invents a CVE/CVSS score. `MultiToolConfirmedCveRule` only ever repeats a CVE id the tools' own output already, verbatim, contained. `KnownVulnerableTechnologyConfirmedByTemplateRule`'s static table only labels *which* known-vulnerable pattern matched — the actual claim that the CVE applies always requires Nuclei's own live confirmation first, never the version match alone (verified by a dedicated test: version match with no Nuclei confirmation produces zero candidates).

**Simplified Scan Experience / Advanced Mode** (`frontend/src/components/executions/ExecuteDialog.tsx`, rewritten): the profile-selection UI was previously hardcoded to a single `nmapProfileId`/`nmapSelected`/literal `"nmap"` string throughout — now generalized to any tool with a profile system via a per-tool `ToolProfilesSelector` subcomponent (one `useScanProfiles(toolName)` call each, React-hooks-legal since it's a separate component instance per selected tool). The simplified default path is now genuinely 3 steps (Scanner(s) -> Target(s) -> Start Assessment) with **no profile UI shown at all** — sending no `tool_options` lets every plugin's own `DEFAULT_PROFILE_ID` apply automatically server-side, which is exactly "every scanner executes using a recommended production profile" with zero extra plumbing. "Advanced Scanner Configuration" is a collapsed-by-default disclosure section; opening it and picking a tool auto-selects that tool's own recommended profile as a starting point (`service_detection`/`default_scan` per plugin) rather than an empty picker. Verified live: Nmap's Advanced section shows "Nmap Profile: Service Detection — 1-5 minutes" pre-selected the instant it's opened.

**Frontend evidence/reference display** (`JobResultsView.tsx`): the existing generic hosts/observations view needed no structural changes to display Nikto/Nuclei results (already tool-agnostic per its own design) — enriched instead with client-side CVE/CWE/severity badge extraction over `observation.detail` (mirroring the backend's own regex extraction, for display only) plus a monospace "evidence viewer" block showing the tool's own captured detail text verbatim, and clickable CVE/CWE badges linking to nvd.nist.gov/cwe.mitre.org.

**Backend tests**: `test_nikto_plugin.py`/`test_nuclei_plugin.py` (27/25 tests — profile loading/categories, command builder including target-resolution and advanced-option overrides, parser against realistic sample XML/JSONL including the CVE-2021-41773 worked example, normalizer including the "never split a title on '.'" regression, profile CRUD), `test_cross_tool_correlation.py` (11 tests — pure rule unit tests plus the full seeded-DB end-to-end test), plus a new HTTP-level regression test in `test_scan_profiles_api.py` locking in the `_to_read` generalization fix. Full suite: 228 passing, 2 skipped (Nikto/Nuclei real-execution tests, correctly skipped since neither binary is installed on this Windows dev machine — see Testing note below).

**Testing note — real execution not demonstrated in this session**: neither `nikto` nor `nuclei` is installed on this Windows development machine (confirmed: `shutil.which` returns `None` for both; no Go toolchain available to `go install` Nuclei either), and per this project's Linux-only runtime architecture, installing them here would be inconsistent with the established edit-on-Windows/run-on-Linux workflow. Parsing/normalization was instead verified against realistic sample output matching each tool's real, documented XML/JSONL schema (including the CVE-2021-41773 worked example verbatim), and the full correlation pipeline was verified against a real database. Both plugins include a `@pytest.mark.skipif`-guarded real-execution test (matching Nmap's own `test_nmap_plugin.py` convention) that will activate automatically once run on a Linux machine with these tools installed — this is the step that still needs doing there, not fabricated here.

**Explicitly not done in Phase 11** (per instructions): no additional scanner integrations. No full Reporting implementation (`backend/reporting/` remains the pre-existing Phase-1 stub — the phase brief's "Reporting: include tool name/execution time/..." describes what a future Reporting phase should include once built, not a mandate to build it now; building a real report generator is a distinct, large undertaking already flagged as its own future phase in this codebase's own docs). No dedicated "Nikto Results"/"Nuclei Results"/full "Template Details" pages — the existing generic `JobResultsView` was deliberately extended in place rather than duplicated, per this phase's own "no duplicated code" instruction; a bespoke Template Details modal was judged to be exactly the kind of tool-specific UI this project's architecture avoids. No full raw `AdvancedOptions` controls (Nmap timing/scripts, Nikto tuning/plugins, Nuclei severity/tags sliders) in the execute dialog — Advanced Mode currently exposes profile selection only; building three bespoke per-tool option forms is a materially larger, separate frontend undertaking than "select a profile," and the Scan Profile system itself already covers the brief's examples (e.g. an SSL-focused Nikto profile achieves the same outcome as manually setting Tuning codes). No full "Filtering" overhaul across every listed dimension (Severity/Tool/Plugin/Template/CVE/CWE/Host/Target/Assessment/Execution) — existing search/filter on Findings/Observations already covers most of these; a dedicated per-dimension filter audit was judged out of scope for a two-plugin integration phase.

## UI/UX Refactor — Remove Internal Data Model Pages

Not a feature phase — a navigation/information-architecture refactor. The primary navigation had grown to expose internal database concepts (Hosts, Host Overview, Services, Technologies, Technology Overview, Observations, Operating Systems) as standalone destinations alongside the actual assessment workflow. This refactor removes all seven as navigation items/routes and makes that same data available contextually instead — nested inside an Assessment's own tabs, an Execution's results, or a Finding's evidence — rather than as independent pages. **Zero backend functionality was touched**: every model, table, API route, normalization/correlation pipeline, and relationship from prior phases remains fully intact; only the frontend's navigation and page structure changed, plus one small additive backend field (below).

**Navigation reduced to exactly seven items** (`src/routes/nav-items.ts`): Dashboard, Assessments, Tools, Executions, Findings, Reports, Settings. "Security Overview" was also removed from the nav array (the brief's "nothing else should appear" is unambiguous), but — since it wasn't named in the explicit removal list and still shows real content nothing else duplicates (severity distribution, critical-findings stat tiles) — its route was kept alive and reachable via a new "Security Overview" button on the Findings page header, rather than deleted or left as an orphaned, unreachable route.

**Eight routes removed** (`src/routes/router.tsx`): `/hosts`, `/hosts/:id`, `/host-overview`, `/services`, `/technologies`, `/technology-overview`, `/observations`, `/operating-systems`, and their eight corresponding page files deleted (`Hosts.tsx`, `HostDetails.tsx`, `HostOverview.tsx`, `Services.tsx`, `Technologies.tsx`, `TechnologyOverview.tsx`, `Observations.tsx`, `OperatingSystems.tsx`).

**Assessment Details gained five new tabs** (`AssessmentDetails.tsx`, alongside the pre-existing Overview/Targets/History): **Executions** (`AssessmentExecutionsPanel.tsx` — the same `JobsTable`/`JobDetailsDialog`/`ExecuteDialog` machinery as the global Executions page, pre-filtered by `assessment_id`), **Findings** (`AssessmentFindingsPanel.tsx` — the same finding-card rendering as the global Findings page, scoped), **Assets Discovered** (`AssessmentAssetsPanel.tsx` — the former `/hosts` list, scoped by `assessment_id`, with a host row expanding a full inline drill-down instead of navigating to a page), **Raw Results** (`AssessmentRawResultsPanel.tsx` — a flat, download-focused list of every completed execution's raw tool output, complementing rather than duplicating the interactive Executions tab), and **Reports** (`AssessmentReportsPanel.tsx` — an assessment-scoped version of the same honest "awaiting completed work" empty state the global Reports page already shows, since report generation itself remains unbuilt).

**The former `HostDetails.tsx` routed page's content was extracted, not deleted**: `HostDetailPanel.tsx` (new) carries the exact same seven sections (Overview/Network Interfaces/Services/Technologies/Operating System/Observations/Execution History) verbatim, now rendered inline wherever a host is selected (currently: the Assets Discovered tab) instead of behind its own URL. `HostTable.tsx` was changed from hardcoded `navigate(/hosts/:id)` to an `onSelectHost` callback prop, so it has no opinion about what selecting a host means — the calling context decides (inline expansion here; a future context could do something else entirely without touching this component).

**One small, additive backend change was required, not merely a frontend one**: `SearchResult` (`backend/schemas/host_inventory.py`) gained `assessment_id: uuid.UUID | None`, populated in `HostInventoryQueryService.search()` (a direct column for host/finding rows; joined through `DiscoveredHost` for service/technology/observation rows, which carry no `assessment_id` of their own). Without this field, "Search results should deep-link into the relevant Assessment" (an explicit requirement) was not implementable at all — the alternative (an extra API round-trip per search-result click to resolve `host_id` -> `assessment_id`) would have been strictly worse. This is additive only: no existing field, route, or behavior was removed, and `backend/tests/test_search.py` (new) locks in the contract with a real, non-nmap-dependent seeded-DB test.

**`Search.tsx`** now deep-links every non-finding result kind (hosts/services/technologies/observations) to `/assessments/:assessment_id` instead of the removed `/hosts/:id`; findings still go straight to `/findings/:id`.

**`FindingDetails.tsx`** gained a real "Affected Host" block on its Overview tab (hostname/IPv4/type/state, satisfying the brief's "Affected Target/Affected Host" requirement more fully than the pre-existing single header chip did) with a link into the discovering assessment's Assets Discovered tab; the header's host chip link was updated the same way.

**`Dashboard.tsx`** was pared down to match the brief's explicit widget list (Assessments/Executions/Findings/Critical Findings/Reports/Tool Status/Recent Activity) and nothing else: removed the entire "Host dashboard" section (OS distribution, technology distribution, top-open-ports, service-distribution charts, and the "Newest hosts" list linking to `/hosts`) and trimmed the Overview stat-tile row from 8 tiles to 4 (Assessments/Targets/Findings/Reports — dropping Hosts/Services/Technologies/Observations counts), since the brief is explicit that raw inventory counts don't belong on the Dashboard. Also dropped the "Host growth"/"Observation trend" trend charts for the same reason, keeping only "Finding trend"/"Execution timeline". Added two widgets the brief asks for that didn't exist before: **Tool status** (reuses `useTools()` + the Phase 10 `ToolOverallStatusBadge`, linking into Tool Management) and **Recent activity** (the 6 most recently created executions across every assessment, reusing `JobStatusBadge`).

**Frontend dead-code cleanup**: `useHostInventory.ts` and `host-inventory-api.ts` pruned to just `useHosts`/`useHost`/`useHostSearch` (the standalone `useServices`/`useTechnologies`/`useObservations`/`useOperatingSystems` hooks and their API wrappers were removed — their only consumers were the eight deleted pages, and the inline host drill-down gets the same data for free from `useHost()`'s already-nested `HostDetail` response). `types/host-inventory.ts` lost the now-unused `ServiceListParams`/`TechnologyListParams`/`ObservationListParams`/`OperatingSystemListParams`/`TECHNOLOGY_CATEGORY_OPTIONS`/`OBSERVATION_CATEGORY_OPTIONS` (the `HOST_TYPE_OPTIONS`/`HOST_STATE_OPTIONS` filter-option constants were kept — reused by the new `AssessmentAssetsPanel`).

**Verified live** through the real browser UI (Playwright, screenshots reviewed): ran a real Nmap scan against `127.0.0.1` inside a fresh test assessment, ran correlation (4 real findings), then confirmed every new surface with real data — sidebar shows exactly the 7 intended items; Assessment Details' Executions/Findings/Assets Discovered/Raw Results/Reports tabs all render real, correct, assessment-scoped data; clicking a host in Assets Discovered expands the full inline drill-down (9 real services, 5 real technologies) with no navigation; Finding Details' new Affected Host block links correctly into the assessment; global search for "localhost" returns a real host result that deep-links to the correct assessment; the Findings page's new Security Overview button correctly reaches the kept-alive `/security-overview` route. Zero console errors and zero 4xx/5xx responses across the entire pass. `tsc -b`/`eslint`/`vite build` all clean (bundle size decreased, as expected from the dead-code removal). Backend: 229 tests passing (one new `test_search.py`, `test_correlation.py`'s cross-tool test suite untouched and still passing), 2 skipped (the pre-existing Nikto/Nuclei real-execution skips).

**Explicitly not done**: no backend models/tables/routes/normalization/correlation logic removed (only additive: one new nullable schema field). No new standalone routes added for host drill-down (`/assessments/:id/hosts/:hostId` was considered and rejected in favor of inline expansion, matching the brief's "everything becomes contextual instead of standalone" instruction literally). No auto-selection of a specific tab/host when deep-linking from Search or Finding Details into an assessment (lands on the assessment's Overview tab; the user picks Assets Discovered themselves) — a nice-to-have, not requested explicitly, and deferred rather than silently multiplying scope.

## Execution Engine Fix — Persist Partial Output on Non-Zero Exit — Complete

Not a phase — a targeted bug fix in `backend/workers/manager.py`. `_execute_job` previously gated `_persist_scan_results` on `exit_code == 0` (`if succeeded:`), so any tool that exited non-zero after already emitting real, usable output on stdout had that output silently discarded — the job's raw output/stdout files still landed on disk, but no `RawToolOutput` row was ever created and no normalization was attempted, so a genuine partial result was invisible to the rest of the platform (no `GET /jobs/{id}/raw-output`, no inventory rows even for the portion a plugin's `normalize()` could have extracted).

Changed the gate from `if succeeded:` to `if raw_output.stdout.strip():` — persistence now depends on whether the tool produced any real output at all, not on its exit code. The job's own status is unaffected (still `COMPLETED`/`FAILED` from the real exit code, with the real error message) — only whether the already-real stdout gets persisted changes. `_persist_scan_results`'s existing try/except around `parse()`/`normalize()` already tolerated a plugin choking on malformed/partial output, so calling it from a FAILED path is safe: nothing gets fabricated, and a tool with truly empty stdout still persists nothing.

**Regression tests** (`backend/tests/test_executions.py`): the dummy execution plugin gained a third failure mode, `"fail-with-output"` (exit code 1 with real, non-empty stdout — simulating a tool that hit a partial error after already producing a genuine partial report), alongside the pre-existing `"fail"` (exit code 1, empty stdout) and `"raise"` (crash) modes. `test_failed_job_with_partial_output_still_persists_raw_output` asserts `GET /jobs/{id}/raw-output` returns 200 with the real partial content for a failed job; `test_failed_job_with_no_output_records_nothing` asserts the companion case (failed, empty stdout) still 404s — locking in both halves of the behavior.

**Not from a real tool run on this session** — surfaced as a design/code-reading fix (a `if succeeded:` gate on data persistence is wrong regardless of which real tool eventually exercises it), not from an actual Nikto/Nuclei execution on this Windows dev machine. The worked justification in the code comment (Nikto's own per-host error-rate limit causing a non-zero exit mid-scan after already emitting a valid partial XML report) describes Nikto's real, documented behavior but was not reproduced live here — real-tool verification remains owed on the Linux target machine, same as Phase 11's still-outstanding Nikto/Nuclei execution tests.

## Phase 12 — Assessment Pipeline & Scanner Orchestration + Intelligent Standard Nmap Profile — Complete

The platform stops being a collection of independently-selected scanners and becomes a real Security Assessment Platform: one click ("Start Assessment") runs Nmap recon, and the platform itself decides — from what Nmap actually discovered — which follow-up scanners make sense, against exactly the endpoints Nmap found (never the original assessment target), chaining automatically through to Correlation. Two connected pieces: **A.** a new Assessment Pipeline Engine, and **B.** a new, genuinely comprehensive "Intelligent Standard" Nmap profile that feeds it real data. **Explicitly deferred (decided with the user before starting):** SSLScan stays detection-only — HTTPS endpoints get Nikto + Nuclei only; building it into a real executing plugin is reserved for its own future phase, exactly like the brief's own SSH/SMB/database scanners.

**A. Pipeline Engine.** New top-level `backend/pipeline/` package, mirroring `backend/correlation/`'s shape exactly: `base.py` (`PipelineRule` ABC — class metadata + one pure `evaluate(service, host)` method, same "self-describing unit" convention as `CorrelationRule`), `models.py` (`ScheduleDecision`/`SkipDecision`/`PipelineDecision`, plus the shared `EXECUTION_STATUS_TO_PIPELINE_JOB_STATUS` mapping), `registry.py` (`PipelineRuleRegistry` — unlike the correlation registry, rule *order* is meaningful: `decide()` returns the first non-`None` verdict, since exactly one rule should ever claim a service), `endpoint_generator.py` (pure `generate_endpoint(host, service, scheme=...)`: port 80/443 omit the port, everything else includes it), and `rules/` (`web_rules.py` — `HttpsServiceRule`/`HttpServiceRule`, schedule Nikto+Nuclei at the generated endpoint; `reserved_rules.py` — `SshReservedRule`/`SmbReservedRule`/`DatabaseReservedRule`, skip with a plain reason, per the brief's explicit "reserve for a future phase" instruction for scanners that don't exist yet).

**Data model** (migration `a4e6f1c2d8b7`): two new tables, `pipeline_runs` (one per "Start Assessment" trigger, mirrors `correlation_runs`' role) and `pipeline_jobs` (one execution-graph node per recon/scan/correlate stage — a fixed 3-tier shape, not a generic dependency graph, since that's the brief's actual shape; a `scan`-stage row either carries a real `execution_id` or stands alone as a pure `SKIPPED` record with a `skip_reason`). `targets` gains two additive columns, `origin` (`user`/`pipeline`) and `discovered_from_execution_id` — lets the Pipeline Engine generate real, synthetic endpoint `Target` rows (e.g. `http://host:80`) that reuse 100% of the existing `ExecutionPlanner`/`ExecutionManager` machinery unmodified (a disabled/uninstalled/unhealthy follow-up scanner gets exactly the same `SKIPPED` treatment a manual `/execute` call would, for free), while staying excluded from the user-facing Targets tab/picker and from `Assessment.target_count`/the Dashboard's own `targets` stat (see Real Bugs below).

**`backend/pipeline/engine.py` (`PipelineEngine`)**: `start()` plans+enqueues one Nmap job (no `profile_id` — relies on Nmap's new `DEFAULT_PROFILE_ID`, part B); `advance_after_nmap()` (called once recon `COMPLETED`) loads every `Service` the execution touched, runs each through the registry, and per decision either schedules the named tool(s) against a get-or-created synthetic target or records a plain skip `Observation` + matching `PipelineJob`(s); a host with zero recognized web services gets one generic `SKIPPED` node each for `nikto`/`nuclei` ("No supported web services discovered.", the brief's own worked example); `advance_after_recon_failure()` handles Nmap `FAILED`/`TIMEOUT`/`CANCELLED` (skip every follow-up with "Nmap did not complete successfully."); `try_finalize()` — once every `scan`-stage job is terminal, calls `CorrelationService.correlate_assessment()` **directly** (never as an event), creates a `correlate`-stage node, marks the run `COMPLETED`.

**Hook**: `backend/workers/pipeline_hook.py`, called from `ExecutionManager._on_job_terminal` (already the precedented place for direct, non-plugin-specific bookkeeping — cohort tracking, assessment status flips). A single indexed-miss query for the overwhelming majority of jobs (every manual "Run Tools" execution isn't part of any pipeline run). This is the one place `backend.workers` reaches toward pipeline/correlation territory — but it calls `backend.pipeline`, and only `backend.pipeline` calls `backend.services.correlation_service`, so Phase 9's explicit rule ("Correlation is never invoked as a side effect of a completed job... never something `backend.workers` calls into") stays true for every **manual** execution; the new automatic call only ever fires for a job that belongs to a `PipelineRun`.

**API**: `POST /assessments/{id}/pipeline/start` (body: `{target_id}`), `GET /assessments/{id}/pipeline` (current/most-recent run + full graph, `PipelineJobRead` denormalizes a `host_label` for display), backed by a new `backend/services/pipeline_service.py` (thin HTTP translation layer, same split as `ExecutionService`/`backend.workers`).

**Frontend**: `ExecuteDialog.tsx` restructured into two views behind an internal `mode` toggle — the new default `PipelineStartView` (target picker only, no tool checklist, calls `pipeline/start`) and `ManualExecuteView` (today's entire original tool-checklist+profile flow, now reachable via "Advanced: run scanners individually instead," satisfying the brief's own Advanced Mode requirement unchanged). New `PipelineGraph.tsx` (`components/pipeline/`) renders the fixed Recon → Scan → Correlate shape as a plain flex/grid layout grouped by host — no DAG library added (confirmed none installed; the shape is fixed, not arbitrary) — wired into `AssessmentDetails.tsx`'s Overview tab. New `usePipeline.ts`/`pipeline-api.ts`/`types/pipeline.ts` follow the exact conventions already established in `useExecutions.ts`/`executions-api.ts`/`types/execution.ts` (same polling shape, same query-key-factory pattern).

**B. Intelligent Standard Nmap Profile.** New `backend/plugins/plugins/nmap/profiles/intelligent_standard.json`: full TCP port scan (`-p 1-65535`), service/version detection (`-sV`), OS detection (`-O`), NSE's own curated `default` category (`-sC` — already excludes intrusive/exploit/brute-force/DoS scripts by NSE's own upstream taxonomy, satisfying the brief's explicit safety requirement), traceroute, and `-T4` aggressive timing (the brief's own "aggressive detection options suitable for assessment" line — see Real Bugs below for why this was added after the fact). `risk_level: "low"`, matching this project's existing convention for scripts confined to `default`/`safe` categories. Zero parser/normalizer changes needed: `backend/plugins/plugins/nmap/parser.py`/`normalizer.py` are already fully script-id-agnostic, so any NSE script `-sC` runs already becomes a generically-categorized `Observation` today. `nmap/plugin.py`'s `DEFAULT_PROFILE_ID` changed from `"service_detection"` to `"intelligent_standard"` — the one line making this the platform's new default for both the manual flow and the Pipeline Engine's own recon job. `service_detection.json` untouched (still selectable, just no longer the default).

**Real bugs found and fixed, all via live testing against this machine's real Nmap install** (not synthetic — see Verification below):

1. **`AmbiguousForeignKeysError` on every model touching `Target`/`ToolExecution`**: adding `Target.discovered_from_execution_id` (FK → `tool_executions.id`) gave those two tables a *second*, opposite-direction FK path (alongside the pre-existing `ToolExecution.target_id`) — SQLAlchemy could no longer infer which relationship should join on which column. Fixed by adding explicit `foreign_keys=` to both `Target.tool_executions` and `ToolExecution.target`. Caught immediately by the full test suite (89 failures on the first run after adding the column) — a textbook case for why "run the whole suite after any schema change" is non-negotiable, not just the new tests.
2. **The `intelligent_standard` profile was originally too slow to be a practical one-click default**: the first cut used `--version-intensity 9` (max) with no timing template at all, so Nmap silently used its own default `-T3`. A real full-port scan against `127.0.0.1` on this dev machine was still running after 5+ minutes. Fixed by adding `-T4` (the brief's own explicit "aggressive detection options suitable for assessment" requirement, missed in the first draft) and dropping the `--version-intensity` override back to Nmap's own default (7) — a real, identical-shape scan then completed in 209 seconds. Caught only by actually running it, not by reading the profile JSON.
3. **The execution graph showed "Waiting" for a job's entire real, multi-minute duration instead of "Running"**: `PipelineJob.status` is only re-stamped at a job's *terminal* transition (the hook only fires there) — nothing updates it on the ordinary PREPARING→RUNNING transition every job goes through. Caught live: the real Nmap recon node sat at "Waiting" in the browser for the whole ~3.5-minute scan. Fixed in `PipelineService._to_read()`: for any job still at `WAITING`, re-derive a live display status from its underlying `ToolExecution`'s *current* status via the same `EXECUTION_STATUS_TO_PIPELINE_JOB_STATUS` mapping, rather than trusting the snapshot taken at creation time. The persisted `PipelineJob.status` column itself is untouched (still correctly authoritative for `try_finalize()`'s "is the scan stage done" check) — only the read/display path changed.
4. **`Assessment.target_count` (and the Dashboard's own `targets` overview stat) counted every synthetic pipeline-generated endpoint target too**, inflating the number shown in the Targets tab's own header well past what the Targets table beneath it — deliberately filtered to `origin == "user"` — actually listed. Caught live: after one real pipeline run, the tab read "Targets (5)" over a table showing 1 row. Fixed by adding the same `origin == "user"` filter to `AssessmentService._read()`'s/`_count_targets_bulk()`'s counting queries and `DashboardService._overview()`'s `targets` count.
5. **A real, substantive `HttpServiceRule` self-containment bug, caught by this phase's own test suite before it ever reached a live run**: `contains_any(name, ("http", "www"))` matched `service_name="https"` too (a plain substring match), so `HttpServiceRule` evaluated standalone (outside `PipelineRuleRegistry`'s own https-first ordering) would incorrectly also claim an HTTPS service. Fixed by explicitly excluding the HTTPS service-name hints first, so the rule is correct on its own merits, not merely correct-by-registry-ordering.
6. **A conflated "any web service recognized" flag caused a double-skip**: the first draft of `_advance_host` only set its "don't fall through to the generic skip" flag on a `ScheduleDecision`, so an SSH-only host got both the correct, specific "SSH service detected..." skip *and* a second, redundant "No supported web services discovered." skip for the same two tools. Caught by this phase's own SSH-only-host test. Fixed by tracking "was this service recognized by any rule at all" (schedule *or* skip), not just "was it scheduled."

**Verification**: `backend/tests/test_pipeline.py` (17 tests) — pure rule/endpoint-generator unit tests (no DB) plus full seeded-DB integration tests driving `PipelineEngine` directly for an HTTP host, an HTTPS host, an SSH-only host, a no-web-service host, a failed-recon host, idempotency across two runs against the same host, the live-status derivation, and the target-count exclusion. Full backend suite: 248 passing, 2 skipped (unchanged pre-existing Nikto/Nuclei real-execution skips). Migration applied to the real dev database (`data/app.db`, backed up first as `data/app.db.bak-phase12-<timestamp>`) — `PRAGMA foreign_key_check` clean, all 33 pre-existing targets correctly backfilled `origin='USER'`.

**Verified live end-to-end** (this machine's real, installed Nmap — not a stub): created a real assessment, added `127.0.0.1` as a target, clicked the new one-click "Start Assessment," watched the graph go Recon (waiting→running→completed) → Scan → Correlate live via Playwright. The real scan discovered a real SMB service (correctly reserved/skipped) and four real HTTP services on this dev machine (ports 8000/9000/9001/24831 — 8000 happens to be the backend's own dev server) — each correctly generated a synthetic endpoint target and scheduled real Nikto+Nuclei attempts, honestly `SKIPPED` since neither tool is installed on this Windows machine (the exact same "tool unavailable" health-check path a manual `/execute` call takes, not a pipeline-specific shortcut). Correlation ran automatically at the end; the assessment flipped to `Completed`. Zero console errors, zero unexpected 4xx/5xx (the one 404 logged is the intentional, handled "no pipeline run yet" response). `tsc -b`/`eslint`/`vite build` all clean.

**Explicitly not done**: SSLScan execution (deferred per the pre-implementation discussion above). No per-run override of which scanners the pipeline runs (Advanced Mode still only offers the pre-existing manual flow, not "disable/force a scanner within an otherwise-automatic pipeline run" — the brief's Advanced Mode section lists this, but building a UI for it is a materially separate undertaking from this phase's core ask). No custom, user-authored pipeline rules/stages. Real Nikto/Nuclei *execution* through the pipeline was not demonstrated (neither is installed on this Windows dev machine) — the scheduling/skip logic around them was fully verified live; actual execution remains owed on the Linux target machine, same standing gap as Phase 11.
